# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Encryption Key (32 bytes base64 encoded)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-32-byte-base64-encoded-key

# GitHub OAuth (optional - for OAuth login flow)
GITHUB_OAUTH_CLIENT_ID=your-github-oauth-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-github-oauth-client-secret

# OpenAI
EMBEDDINGS_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-api-key

# Claude Code Documentation Generation
# Choose ONE option:
#
# Option 1: Mock runner for development (default - generates sample docs)
USE_MOCK_CLAUDE=true
#
# Option 2: Anthropic API (recommended for production)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-api-key
#
# Option 3: Claude CLI (experimental - not recommended for server use)
# Requires `claude` CLI installed globally and authenticated
# USE_CLAUDE_CLI=true
